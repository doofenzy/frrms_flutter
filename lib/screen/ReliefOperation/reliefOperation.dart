import 'package:flutter/material.dart';

class Reliefoperation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the relief operation Screen',
          style: TextStyle(fontSize: 24)),
    );
  }
}
