import 'package:flutter/material.dart';

class Buenavistamap extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the buenavista map Screen',
          style: TextStyle(fontSize: 24)),
    );
  }
}
