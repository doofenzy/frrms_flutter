import 'package:flutter/material.dart';

class Sitrep extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the sitrep Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
