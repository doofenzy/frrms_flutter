import 'package:flutter/material.dart';

class ReliefOperation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the relief Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
