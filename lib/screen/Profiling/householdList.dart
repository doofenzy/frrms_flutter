import 'package:flutter/material.dart';

class Householdlist extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child:
          Text('This is the household Screen', style: TextStyle(fontSize: 24)),
    );
  }
}
